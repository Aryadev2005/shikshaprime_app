export interface Assignment {
    id: string;
    title: string;
    description: string;
    dueDate: Date;
    createdAt: Date;
    updatedAt: Date;
}

export const assignmentModel = {
    // Define any methods related to the assignment model here
};