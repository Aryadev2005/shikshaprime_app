# Server
PORT=4000
NODE_ENV=development

# Database (ShikshaPrime MySQL — same as all existing services)
DB_HOST=69.62.84.110
DB_PORT=3306
DB_NAME=shikshaprime_main
DB_USERNAME=root
DB_PASSWORD=

# JWT
JWT_SECRET=mivdjh32hjfdgppkmdu8

# CORS — comma-separated list
ALLOWED_ORIGINS=http://localhost:8081,http://localhost:19000,http://localhost:19006,exp://localhost:8081

# Email (ZeptoMail — same as ShikshaPrime)
EMAIL_HOST=smtp.zeptomail.in
EMAIL_PORT=587
EMAIL_USER=emailapikey
EMAIL_PASS=
EMAIL_FROM=noreply@instivera.com

# PhonePe (optional — stub mode used if not set)
PHONEPE_MERCHANT_ID=
PHONEPE_SALT_KEY=
PHONEPE_SALT_INDEX=1
PHONEPE_BASE_URL=https://api.phonepe.com/apis/hermes

# Logging
LOG_LEVEL=info
